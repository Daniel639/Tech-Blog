<div class="post-list">
  {{#each posts as |post|}}
  <div class="post-container">
    <h2>{{post.title}}</h2>
    <p>Posted by {{post.user.username}} on {{format_date post.createdAt}}</p>
    <p>{{post.content}}</p>
  </div>
  {{/each}}
</div>